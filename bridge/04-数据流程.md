# 04 - 数据流程

> **适合人群**: 开发者  
> **阅读时间**: 20 分钟

---

## 📖 目录

1. [Peggy 存款流程](#peggy-存款流程)
2. [Peggy 提现流程](#peggy-提现流程)
3. [Axelar 桥接流程](#axelar-桥接流程)
4. [余额查询流程](#余额查询流程)
5. [授权流程](#授权流程)

---

## 🔵 Peggy 存款流程

### 场景：以太坊 → Injective

**用户操作**：转移 100 USDT 从以太坊到 Injective

### 时序图

```
用户        BridgeFromV2    PeggyProvider    web3Composer    WalletService    以太坊        Injective
 │              │                │                 │               │             │             │
 │──点击Bridge──>│                │                 │               │             │             │
 │              │──peggyEthDeposit─>│                │               │             │             │
 │              │                │──检查授权额度──────>│               │             │             │
 │              │                │<─授权不足──────────│               │             │             │
 │              │                │──调用approve()────>│──签名交易──────>│──广播tx───>│             │
 │              │                │                 │               │<───confirmed│             │
 │              │                │<─授权成功──────────│               │             │             │
 │              │                │──构建存款交易────────>│               │             │             │
 │              │                │<─返回tx data───────│               │             │             │
 │              │                │──调用deposit()────>│──签名交易──────>│──广播tx───>│             │
 │              │                │                 │               │<───pending──│             │
 │              │                │                 │               │             │──验证器监听──>│
 │              │                │                 │               │             │<───confirmed│
 │              │                │                 │               │             │──铸造代币──>│
 │              │<─返回tx hash───│                 │               │             │             │
 │<──显示成功────│                │                 │               │             │             │
```

### 详细步骤

#### 第 1 步：用户发起操作

```typescript
// 用户在 BridgeFromV2 组件填写表单
const handleSubmit = async (data: FormData) => {
  const { amount } = data
  const token = ethUsdtToken // USDT
  
  // 调用 Peggy Provider
  await peggyEthDeposit({
    amount: "100",
    token: ethUsdtToken
  })
}
```

#### 第 2 步：检查授权（Allowance）

```typescript
// PeggyProvider.tsx
const peggyEthDeposit = async ({ amount, token }) => {
  // 1. 检查当前授权额度
  const allowance = await checkAllowance(token.address)
  
  // 2. 如果授权不足，先授权
  if (allowance < amount) {
    await approveToken(token.address, UNLIMITED_ALLOWANCE)
  }
  
  // 3. 继续存款流程
  ...
}
```

**为什么需要授权？**
- ERC20 代币需要授权才能被合约使用
- 授权是一次性的（通常授权无限额度）
- 后续转账无需重复授权

#### 第 3 步：授权交易（Approve）

```typescript
// useWagmiContracts.ts
const { writeContractAsync } = useWriteContract()

// 调用 ERC20 的 approve 方法
const approveTx = await writeContractAsync({
  address: tokenAddress,           // USDT 合约地址
  abi: erc20Abi,                   // ERC20 ABI
  functionName: 'approve',
  args: [
    peggyContractAddress,          // Peggy 合约地址
    MaxUint256                     // 无限授权
  ]
})

// 等待交易确认
await waitForTransactionReceipt({
  hash: approveTx
})
```

**Gas 费用**：~$5-20（以太坊主网）

#### 第 4 步：构建存款交易

```typescript
// web3composer.ts
const msg = web3Composer.getPeggyDepositMsg({
  amount: "100000000",              // 100 USDT (6 decimals)
  address: ethereumAddress,         // 用户的以太坊地址
  denom: token.denom,               // peggy0xdAC17F958D2ee523a2206206994597C13D831ec7
  injectiveAddress: injectiveAddress // 用户的 Injective 地址
})
```

**重要参数**：
- `amount`：必须转换为最小单位（考虑精度）
- `denom`：Peggy 代币的标识符
- `injectiveAddress`：目标 Injective 地址

#### 第 5 步：调用 Peggy 合约

```typescript
// useWagmiContracts.ts
const depositTx = await writeContractAsync({
  address: peggyContractAddress,
  abi: peggyAbi,
  functionName: 'sendToInjective',
  args: [
    tokenAddress,                  // USDT 合约地址
    injectiveAddress,              // 目标地址（Injective 格式）
    amountInWei                    // 转账金额
  ]
})
```

**合约方法**：
```solidity
function sendToInjective(
  address _tokenContract,
  string memory _destination,
  uint256 _amount
) external
```

**Gas 费用**：~$10-50（以太坊主网）

#### 第 6 步：等待跨链确认

```
以太坊区块确认（12个区块，~3分钟）
        ↓
验证器监听事件（实时）
        ↓
验证器达成共识（~2分钟）
        ↓
在 Injective 铸造代币（~1分钟）
        ↓
用户收到代币
```

**总时间**：5-15 分钟

#### 第 7 步：铸造完成

```typescript
// 用户在 Injective 钱包看到余额
Before: 0 USDT
After:  100 USDT (peggy0xdAC17...ec7)
```

---

## 🔴 Peggy 提现流程

### 场景：Injective → 以太坊

**用户操作**：提现 50 USDT 从 Injective 到以太坊

### 时序图

```
用户        BridgeFromV2    PeggyProvider    msgBroadcaster    Injective    验证器集    以太坊
 │              │                │                 │               │           │          │
 │──点击Bridge──>│                │                 │               │           │          │
 │              │──peggyInjectiveToEth─>│                │               │           │          │
 │              │                │──构建提现消息──────>│               │           │          │
 │              │                │<─返回msg───────────│               │           │          │
 │              │                │──广播交易──────────>│──提交tx────>│           │          │
 │              │                │                 │<───confirmed──│           │          │
 │              │                │                 │               │──销毁代币─>│          │
 │              │                │                 │               │──通知验证器>│          │
 │              │                │                 │               │           │──批量处理─>│
 │              │                │                 │               │           │──释放代币>│
 │              │<─返回tx hash───│                 │               │           │          │
 │<──显示成功────│                │                 │               │           │          │
```

### 详细步骤

#### 第 1 步：构建提现消息

```typescript
// PeggyProvider.tsx
const peggyInjectiveToEth = async ({ amount, token }) => {
  const msg = web3Composer.getPeggyWithdrawMsg({
    amount: "50000000",            // 50 USDT
    address: ethereumAddress,      // 目标以太坊地址
    denom: token.denom,            // peggy0xdAC17F958D2ee523a2206206994597C13D831ec7
    injectiveAddress: injectiveAddress,
    bridgeFee: "1000000"           // 桥接费用（1 USDT）
  })
  
  return msg
}
```

**桥接费用（Bridge Fee）**：
- 支付给验证器的费用
- 通常为 1-2 USDT
- 用于激励验证器处理跨链交易

#### 第 2 步：签名并广播

```typescript
// walletService.ts
const txResponse = await msgBroadcaster.broadcast({
  msgs: [msg],
  injectiveAddress: injectiveAddress
})

console.log('Transaction hash:', txResponse.txHash)
```

**Injective Gas 费**：~$0.01（非常便宜）

#### 第 3 步：Injective 端处理

```
1. 销毁用户的 Peggy USDT
2. 记录提现请求
3. 通知验证器集
```

#### 第 4 步：验证器批量处理

```
验证器每 4-12 小时批量处理一次提现
     ↓
收集所有提现请求
     ↓
生成批量交易
     ↓
多重签名
     ↓
提交到以太坊
```

**批量处理的原因**：
- 节省 Gas 费用
- 提高效率
- 多个用户共享成本

#### 第 5 步：以太坊释放代币

```solidity
// Peggy 合约在以太坊释放代币
function submitBatch(
  address[] memory _currentValidators,
  uint256[] memory _currentPowers,
  uint256[] memory _amounts,
  address[] memory _destinations,
  // ...
) external
```

用户在以太坊钱包收到原始的 ERC20 USDT

**总时间**：5-24 小时（取决于批量处理周期）

---

## 🟢 Axelar 桥接流程

### 场景：BSC → Injective

**用户操作**：转移 100 USDT 从 BSC 到 Injective

### 时序图

```
用户        BridgeFromV2    AxelarProvider    SquidSDK    BSC    Axelar网络    Injective
 │              │                │               │        │         │             │
 │──点击Bridge──>│                │               │        │         │             │
 │              │──axelarEvmDeposit─>│               │        │         │             │
 │              │                │──构建路由──────>│        │         │             │
 │              │                │<─返回route─────│        │         │             │
 │              │                │──执行交易──────>│        │         │             │
 │              │                │                │──调用合约>│         │             │
 │              │                │                │<───tx hash│         │             │
 │              │                │                │        │──验证────>│             │
 │              │                │                │        │<─confirmed│             │
 │              │                │                │        │         │──IBC传输────>│
 │              │                │                │        │         │             │──铸造代币──>│
 │              │<─返回tx hash───│                │        │         │             │
 │<──显示成功────│                │                │        │         │             │
```

### 详细步骤

#### 第 1 步：计算最优路由

```typescript
// AxelarProvider.tsx
import { Squid } from "@0xsquid/sdk"

const squid = new Squid({
  baseUrl: "https://api.0xsquid.com",
  integratorId: "your-integrator-id"
})

// 获取路由
const route = await squid.getRoute({
  fromChain: "binance",              // BSC
  toChain: "injective",              // Injective
  fromToken: bnbUsdtToken.address,   // BSC USDT
  toToken: injAxlUsdcToken.denom,    // INJ axlUSDC
  fromAmount: "100000000000000000000", // 100 USDT (18 decimals)
  fromAddress: ethereumAddress,
  toAddress: injectiveAddress
})
```

**Squid SDK**：
- Axelar 的路由聚合器
- 自动选择最优路径
- 处理多跳转账

#### 第 2 步：检查并授权

```typescript
// 与 Peggy 类似
if (needsApproval) {
  await approveToken(tokenAddress, UNLIMITED_ALLOWANCE)
}
```

#### 第 3 步：执行跨链交易

```typescript
const tx = await squid.executeRoute({
  signer: ethersSigner,
  route: route
})

await tx.wait()
```

**Gas 费用**：~$1-5（BSC 便宜很多）

#### 第 4 步：Axelar 网络处理

```
BSC 锁定代币
    ↓
Axelar 验证器确认（~3-10分钟）
    ↓
通过 IBC 传输到 Injective
    ↓
在 Injective 铸造 axlUSDC
```

**协议费**：0.1%-0.3%

**总时间**：5-30 分钟

---

## 💰 余额查询流程

### 场景：查询用户的代币余额

```typescript
// AccountProvider.tsx
const fetchBalanceAndAllowance = async () => {
  // 1. 查询以太坊余额
  const ethBalance = await web3Client.fetchBalance(
    address,
    ethUsdtToken.address
  )
  
  // 2. 查询 BSC 余额
  const bnbBalance = await web3Client.fetchBalance(
    address,
    bnbUsdtToken.address,
    BNB_CHAIN_ID
  )
  
  // 3. 查询 Injective 余额
  const injBalances = await indexerGrpcAccountPortfolioApi
    .fetchAccountPortfolio(injectiveAddress)
  
  // 4. 更新状态
  setDenomBalanceMap({
    [ethUsdtToken.denom]: ethBalance,
    [bnbUsdtToken.address]: bnbBalance,
    ...injBalances
  })
}
```

### 余额存储结构

```typescript
interface BalanceMap {
  [denom: string]: {
    balance: string        // 余额（最小单位）
    inUsd: string         // USD 价值
    denom: string         // 代币标识
  }
}

// 示例
denomBalanceMap = {
  "peggy0xdAC17F958D2ee523a2206206994597C13D831ec7": {
    balance: "100000000",    // 100 USDT
    inUsd: "100.00",
    denom: "peggy0xdAC17F958D2ee523a2206206994597C13D831ec7"
  },
  "inj": {
    balance: "50000000000000000000",  // 50 INJ
    inUsd: "1000.00",
    denom: "inj"
  }
}
```

---

## ✅ 授权流程

### ERC20 授权机制

#### 为什么需要授权？

```
你的钱包
    ├── 100 USDT
    └── 你的控制

如果 Peggy 合约想要转移你的 USDT：
    ❌ 默认：无权限
    ✅ 授权后：可以转移（在授权额度内）
```

#### 授权步骤

```typescript
// 1. 查询当前授权额度
const allowance = await erc20Contract.read.allowance([
  userAddress,          // 用户地址
  peggyContractAddress  // Peggy 合约地址
])

// 2. 如果授权不足，调用 approve
if (allowance < amount) {
  const tx = await erc20Contract.write.approve([
    peggyContractAddress,
    MaxUint256           // 无限授权
  ])
  
  await tx.wait()
}

// 3. 现在可以调用 Peggy 合约了
await peggyContract.write.sendToInjective([...])
```

#### 无限授权 vs 精确授权

| 方式 | 优点 | 缺点 |
|------|------|------|
| 无限授权 | 只需授权一次 | 理论上有安全风险 |
| 精确授权 | 更安全 | 每次都要授权，Gas 费高 |

**推荐**：使用无限授权（官方合约是安全的）

---

## 🔄 完整流程总结

### Peggy 存款（ETH → INJ）

```
1. 用户填写表单（10秒）
2. 检查授权额度（5秒）
3. [首次] 授权 ERC20（1-3分钟，$5-20）
4. 调用 Peggy.sendToInjective（1-3分钟，$10-50）
5. 等待以太坊确认（12区块，~3分钟）
6. 验证器处理（~2分钟）
7. Injective 铸造（~1分钟）
───────────────────────────────────
总时间：5-15分钟
总费用：$15-70（首次）/ $10-50（后续）
```

### Peggy 提现（INJ → ETH）

```
1. 用户填写表单（10秒）
2. 广播 Injective 交易（5秒，$0.01）
3. Injective 确认并销毁（~1分钟）
4. 等待验证器批量处理（4-12小时）
5. 验证器提交到以太坊（~5分钟）
6. 以太坊释放代币（~3分钟）
───────────────────────────────────
总时间：5-24小时
总费用：$0.01（Injective）+ 1-2 USDT（桥接费）
```

### Axelar 桥接（BSC → INJ）

```
1. 用户填写表单（10秒）
2. 检查授权额度（5秒）
3. [首次] 授权 BEP20（10-30秒，$0.1-0.5）
4. 执行 Squid 路由（30-60秒，$1-5）
5. BSC 确认（~1分钟）
6. Axelar 验证（3-10分钟）
7. IBC 传输到 Injective（2-5分钟）
8. Injective 铸造（~1分钟）
───────────────────────────────────
总时间：5-30分钟
总费用：$1-6 + 0.1%-0.3%
```

---

## 📖 下一步

- 👉 [组件说明](./05-组件说明.md) - 查看代码实现
- 👉 [API 参考](./06-API参考.md) - API 文档
- 👉 [常见问题](./07-常见问题.md) - FAQ

---

*最后更新: 2025-10-30*

