# biya-helix-app 迁移 pnpm 快速操作指南

## 5分钟快速迁移

### 前提条件
- ✅ 已备份代码（git commit）
- ✅ pnpm 已安装（`pnpm --version`）

---

## 执行命令（复制粘贴即可）

```powershell
# 进入项目目录
cd D:\rwa\biya-coin\biya-helix-app

# 创建 .npmrc 配置
@"
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
"@ | Out-File -FilePath .npmrc -Encoding utf8

# 清理旧依赖
Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
Remove-Item -Force package-lock.json -ErrorAction SilentlyContinue

# 安装依赖
pnpm install

# 测试运行
pnpm dev
```

---

## 验证步骤

1. **访问页面**：http://localhost:8080
2. **检查功能**：钱包连接、交易等核心功能
3. **查看控制台**：确保没有错误

---

## 如果成功

```bash
# 提交更改
git add .
git commit -m "迁移到 pnpm"
```

---

## 如果失败（回滚）

```powershell
cd D:\rwa\biya-coin\biya-helix-app
Remove-Item -Recurse -Force node_modules
Remove-Item -Force pnpm-lock.yaml
Remove-Item -Force .npmrc
git checkout package-lock.json
npm install
npm run dev
```

---

## 常用命令

| 操作 | 命令 |
|-----|------|
| 安装依赖 | `pnpm install` |
| 添加包 | `pnpm add 包名` |
| 删除包 | `pnpm remove 包名` |
| 运行开发 | `pnpm dev` |
| 构建项目 | `pnpm build` |
| 启动生产 | `pnpm start` |

---

## 遇到问题？

查看详细文档：`biya-helix-app迁移到pnpm.md`

