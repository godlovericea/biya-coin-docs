# 📂 配置文件详解

> **核心内容**: TypeScript、Next.js、Tailwind、Workspace 配置  
> **目标**: 理解和配置 Monorepo 所有关键文件  
> **更新时间**: 2025-10-31

---

## 📖 目录

1. [根目录配置](#根目录配置)
2. [TypeScript 配置](#typescript-配置)
3. [Next.js 配置](#nextjs-配置)
4. [Tailwind CSS 配置](#tailwind-css-配置)
5. [国际化配置](#国际化配置)
6. [部署配置](#部署配置)

---

## 🌳 根目录配置

### 1. package.json（根配置）

```json
{
  "name": "biya-coin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "// 开发模式": "",
    "dev": "npm run dev --workspaces --if-present",
    "dev:helix": "npm run dev --workspace=apps/helix",
    "dev:bridge": "npm run dev --workspace=apps/bridge",
    "dev:dex": "npm run dev --workspace=apps/dex",
    
    "// 构建": "",
    "build": "npm run build --workspaces --if-present",
    "build:helix": "npm run build --workspace=apps/helix",
    "build:bridge": "npm run build --workspace=apps/bridge",
    "build:dex": "npm run build --workspace=apps/dex",
    
    "// 启动": "",
    "start:helix": "npm run start --workspace=apps/helix",
    "start:bridge": "npm run start --workspace=apps/bridge",
    
    "// 测试": "",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    
    "// 清理": "",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "clean:build": "rm -rf apps/*/.next packages/*/dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
```

---

### 2. pnpm-workspace.yaml（pnpm 配置）

```yaml
packages:
  - 'apps/*'
  - 'packages/*'

# 可选：排除某些目录
# exclude:
#   - '**/.next/**'
#   - '**/dist/**'
```

---

### 3. .gitignore

```gitignore
# 依赖
node_modules/
.pnp
.pnp.js

# 构建产物
.next/
out/
dist/
build/

# 缓存
.turbo/
.cache/

# 环境变量
.env
.env*.local

# 调试
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# 测试
coverage/

# Vercel
.vercel
```

---

## 📘 TypeScript 配置

### 1. tsconfig.json（根配置）

```json
{
  "compilerOptions": {
    // 编译选项
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    
    // 严格模式
    "strict": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "noImplicitAny": true,
    
    // 模块解析
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    
    // JSX
    "jsx": "preserve",
    "jsxImportSource": "react",
    
    // 路径
    "baseUrl": ".",
    "paths": {
      "@biya/shared": ["./packages/shared"],
      "@biya/shared/*": ["./packages/shared/*"],
      "@biya/theme": ["./packages/theme"],
      "@biya/theme/*": ["./packages/theme/*"],
      "@biya/i18n": ["./packages/i18n"],
      "@biya/i18n/*": ["./packages/i18n/*"],
      "@biya/wallet": ["./packages/wallet"],
      "@biya/wallet/*": ["./packages/wallet/*"],
      "@biya/auth": ["./packages/auth"],
      "@biya/auth/*": ["./packages/auth/*"]
    },
    
    // 输出
    "skipLibCheck": true,
    "noEmit": true,
    "incremental": true,
    
    // 其他
    "allowJs": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": [
    "apps/**/*",
    "packages/**/*"
  ],
  "exclude": [
    "node_modules",
    ".next",
    "dist",
    "out"
  ]
}
```

---

### 2. apps/helix/tsconfig.json

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@biya/shared": ["../../packages/shared"],
      "@biya/theme": ["../../packages/theme"],
      "@biya/i18n": ["../../packages/i18n"],
      "@biya/wallet": ["../../packages/wallet"],
      "@biya/auth": ["../../packages/auth"]
    },
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
```

---

### 3. apps/bridge/tsconfig.json

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"],
      "@biya/shared": ["../../packages/shared"],
      "@biya/theme": ["../../packages/theme"],
      "@biya/i18n": ["../../packages/i18n"],
      "@biya/wallet": ["../../packages/wallet"]
    },
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
```

---

### 4. packages/shared/tsconfig.json

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },
    "composite": true,
    "declaration": true,
    "declarationMap": true,
    "outDir": "./dist"
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
```

---

## ⚙️ Next.js 配置

### 1. apps/helix/next.config.ts

```typescript
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'

const withNextIntl = createNextIntlPlugin('./i18n/request.ts')

const nextConfig: NextConfig = {
  // 严格模式
  reactStrictMode: true,
  
  // 实验性特性
  experimental: {
    optimizePackageImports: [
      '@biya/shared',
      '@biya/theme',
      '@biya/i18n',
      'lucide-react'
    ],
  },
  
  // 图片优化
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**.example.com',
      },
    ],
  },
  
  // TypeScript
  typescript: {
    ignoreBuildErrors: false,
  },
  
  // ESLint
  eslint: {
    ignoreDuringBuilds: false,
  },
  
  // 国际化
  // i18n 通过 next-intl 处理，不需要这里配置
  
  // 重定向
  async redirects() {
    return [
      // 示例重定向
    ]
  },
  
  // 重写
  async rewrites() {
    return [
      // 示例重写
    ]
  },
  
  // 环境变量
  env: {
    CUSTOM_KEY: process.env.CUSTOM_KEY,
  },
}

export default withNextIntl(nextConfig)
```

---

### 2. apps/bridge/next.config.ts

```typescript
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'
import path from 'path'

const withNextIntl = createNextIntlPlugin('./i18n/request.ts')

const nextConfig: NextConfig = {
  reactStrictMode: true,
  
  experimental: {
    optimizePackageImports: [
      '@biya/shared',
      '@biya/theme',
      '@biya/i18n',
      '@biya/wallet',
      'lucide-react'
    ],
  },
  
  // Webpack 配置（处理 vendor 包）
  webpack: (config, { isServer }) => {
    // 添加 vendor 别名
    config.resolve.alias = {
      ...config.resolve.alias,
      '@injectivelabs/core-proto-ts': path.resolve(
        __dirname,
        '../../vendor/injective/core-proto-ts'
      ),
    }
    
    // Node.js polyfills
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        crypto: require.resolve('crypto-browserify'),
        stream: require.resolve('stream-browserify'),
        buffer: require.resolve('buffer/'),
        util: require.resolve('util/'),
        process: require.resolve('process/browser'),
      }
    }
    
    return config
  },
  
  // 转译包
  transpilePackages: [
    '@injectivelabs/sdk-ts',
    '@injectivelabs/wallet-strategy',
    '@injectivelabs/wallet-ts',
    '@0xsquid/sdk',
  ],
}

export default withNextIntl(nextConfig)
```

---

## 🎨 Tailwind CSS 配置

### 1. apps/helix/tailwind.config.js

```javascript
import type { Config } from 'tailwindcss'

const config: Config = {
  // 内容路径
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    // 包含共享包
    '../../packages/shared/**/*.{js,ts,jsx,tsx}',
    '../../packages/theme/**/*.{js,ts,jsx,tsx}',
  ],
  
  // 暗色模式
  darkMode: 'class',
  
  theme: {
    extend: {
      colors: {
        // 自定义颜色
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
          950: '#082f49',
        },
        // 从 @biya/theme 导入
        background: 'var(--background)',
        foreground: 'var(--foreground)',
      },
      
      fontFamily: {
        sans: ['var(--font-geist-sans)'],
        mono: ['var(--font-geist-mono)'],
      },
      
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      
      keyframes: {
        'fade-in': {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        'slide-up': {
          '0%': { transform: 'translateY(10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },
      
      animation: {
        'fade-in': 'fade-in 0.3s ease-out',
        'slide-up': 'slide-up 0.3s ease-out',
      },
    },
  },
  
  plugins: [],
}

export default config
```

---

### 2. apps/bridge/tailwind.config.js

```javascript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    '../../packages/shared/**/*.{js,ts,jsx,tsx}',
    '../../packages/theme/**/*.{js,ts,jsx,tsx}',
    '../../packages/wallet/**/*.{js,ts,jsx,tsx}',
  ],
  
  darkMode: 'class',
  
  theme: {
    extend: {
      colors: {
        // Bridge 特定颜色
        bridge: {
          from: '#3b82f6',
          to: '#8b5cf6',
        },
        ethereum: '#627eea',
        injective: '#00d2ff',
        bsc: '#f3ba2f',
        
        // 共享颜色
        background: 'var(--background)',
        foreground: 'var(--foreground)',
      },
      
      backgroundImage: {
        'gradient-radial': 'radial-gradient(var(--tw-gradient-stops))',
        'gradient-bridge': 'linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%)',
      },
    },
  },
  
  plugins: [],
}

export default config
```

---

### 3. 共享 Tailwind 配置（可选）

```javascript
// packages/config/tailwind-config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        // 共享颜色定义
        brand: {
          50: '#f0f9ff',
          // ...
        },
      },
    },
  },
  plugins: [],
}

// apps/*/tailwind.config.js 中使用
import sharedConfig from '../../packages/config/tailwind-config'

export default {
  ...sharedConfig,
  content: [
    // ...
  ],
}
```

---

## 🌍 国际化配置

### 1. packages/i18n/config/locales.ts

```typescript
export const locales = ['en', 'zh', 'ja', 'ko', 'es'] as const
export type Locale = (typeof locales)[number]

export const defaultLocale: Locale = 'en'

export const localeNames: Record<Locale, string> = {
  en: 'English',
  zh: '中文',
  ja: '日本語',
  ko: '한국어',
  es: 'Español',
}

export const localeFlags: Record<Locale, string> = {
  en: '🇺🇸',
  zh: '🇨🇳',
  ja: '🇯🇵',
  ko: '🇰🇷',
  es: '🇪🇸',
}
```

---

### 2. apps/helix/i18n/request.ts

```typescript
import { getRequestConfig } from 'next-intl/server'
import { notFound } from 'next/navigation'
import { locales } from '@biya/i18n/config/locales'

export default getRequestConfig(async ({ locale }) => {
  // 验证 locale
  if (!locales.includes(locale as any)) {
    notFound()
  }

  return {
    messages: (await import(`../messages/${locale}.json`)).default,
    timeZone: 'Asia/Shanghai',
    now: new Date(),
  }
})
```

---

### 3. apps/bridge/i18n/request.ts

```typescript
import { getRequestConfig } from 'next-intl/server'
import { notFound } from 'next/navigation'
import { locales, mergeTranslations } from '@biya/i18n'

export default getRequestConfig(async ({ locale }) => {
  if (!locales.includes(locale as any)) {
    notFound()
  }

  // 合并分层翻译
  const baseMessages = mergeTranslations(locale, [
    'common',   // 通用
    'wallet',   // 钱包
    'bridge',   // 桥接领域
  ])
  
  // 应用特定翻译
  const appMessages = (await import(`../messages/${locale}.json`)).default
  
  return {
    messages: {
      ...baseMessages,
      ...appMessages,
    },
    timeZone: 'Asia/Shanghai',
    now: new Date(),
  }
})
```

---

### 4. middleware.ts（根目录）

```typescript
import createMiddleware from 'next-intl/middleware'
import { locales, defaultLocale } from './packages/i18n/config/locales'

export default createMiddleware({
  locales,
  defaultLocale,
  localePrefix: 'as-needed',
})

export const config = {
  // 匹配所有路径，除了 api、_next、静态文件
  matcher: [
    '/((?!api|_next|_vercel|.*\\..*).*)',
  ],
}
```

---

## 🚀 部署配置

### 1. apps/helix/vercel.json

```json
{
  "buildCommand": "cd ../.. && npm run build:helix",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "env": {
    "NEXT_PUBLIC_API_URL": "@api-url",
    "NEXT_PUBLIC_SITE_URL": "@site-url"
  },
  "regions": ["hnd1", "sfo1"],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        }
      ]
    }
  ]
}
```

---

### 2. apps/bridge/vercel.json

```json
{
  "buildCommand": "cd ../.. && npm run build:bridge",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install",
  "devCommand": "npm run dev -- -p 3001",
  "env": {
    "NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID": "@wallet-connect-project-id",
    "NEXT_PUBLIC_ALCHEMY_KEY": "@alchemy-key"
  },
  "regions": ["hnd1", "sfo1"]
}
```

---

### 3. .github/workflows/deploy-helix.yml

```yaml
name: Deploy Helix

on:
  push:
    branches:
      - main
    paths:
      - 'apps/helix/**'
      - 'packages/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Helix
        run: npm run build:helix
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID_HELIX }}
          working-directory: ./apps/helix
          vercel-args: '--prod'
```

---

### 4. .github/workflows/deploy-bridge.yml

```yaml
name: Deploy Bridge

on:
  push:
    branches:
      - main
    paths:
      - 'apps/bridge/**'
      - 'packages/shared/**'
      - 'packages/theme/**'
      - 'packages/i18n/**'
      - 'packages/wallet/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Bridge
        run: npm run build:bridge
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID_BRIDGE }}
          working-directory: ./apps/bridge
          vercel-args: '--prod'
```

---

## 📦 Package 配置示例

### packages/shared/package.json

```json
{
  "name": "@biya/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    },
    "./components": {
      "types": "./components/index.ts",
      "default": "./components/index.ts"
    },
    "./components/*": {
      "types": "./components/*.tsx",
      "default": "./components/*.tsx"
    },
    "./hooks": {
      "types": "./hooks/index.ts",
      "default": "./hooks/index.ts"
    },
    "./utils": {
      "types": "./utils/index.ts",
      "default": "./utils/index.ts"
    }
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint ."
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.3.1",
    "lucide-react": "^0.548.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.0.0"
  }
}
```

---

### packages/theme/package.json

```json
{
  "name": "@biya/theme",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "types": "./index.ts",
      "default": "./index.ts"
    },
    "./store": {
      "types": "./store/theme-store.ts",
      "default": "./store/theme-store.ts"
    },
    "./components": {
      "types": "./components/index.ts",
      "default": "./components/index.ts"
    }
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "dependencies": {
    "zustand": "^5.0.8"
  }
}
```

---

## 📋 配置检查清单

### 根目录

- [ ] `package.json` - Workspaces 配置
- [ ] `pnpm-workspace.yaml` - pnpm 配置（如使用 pnpm）
- [ ] `tsconfig.json` - TypeScript 根配置
- [ ] `.gitignore` - Git 忽略文件
- [ ] `.eslintrc.js` - ESLint 配置
- [ ] `.prettierrc` - Prettier 配置

### 应用配置（apps/*）

- [ ] `package.json` - 应用依赖和脚本
- [ ] `next.config.ts` - Next.js 配置
- [ ] `tsconfig.json` - TypeScript 配置
- [ ] `tailwind.config.js` - Tailwind 配置
- [ ] `vercel.json` - Vercel 部署配置
- [ ] `i18n/request.ts` - 国际化配置

### 共享包配置（packages/*）

- [ ] `package.json` - 包配置和导出
- [ ] `tsconfig.json` - TypeScript 配置
- [ ] `index.ts` - 统一导出

### CI/CD

- [ ] `.github/workflows/*.yml` - GitHub Actions
- [ ] Vercel 项目配置
- [ ] 环境变量设置

---

## 🚀 下一步

配置文件已明确，继续阅读：

**→ [04-共享包设计.md](./04-共享包设计.md)**

了解如何设计和实现共享包（shared、theme、i18n、wallet、auth）。

---

## 📚 本章小结

### 核心配置

1. **Workspace**: package.json + pnpm-workspace.yaml
2. **TypeScript**: 根配置 + 应用配置 + 包配置
3. **Next.js**: 应用特定配置 + Webpack 自定义
4. **Tailwind**: 共享配置 + 应用定制
5. **国际化**: next-intl + 分层翻译
6. **部署**: Vercel + GitHub Actions

### 配置原则

1. **根配置**: 通用设置，被继承
2. **应用配置**: 基于根配置扩展
3. **包配置**: 独立但兼容
4. **环境变量**: 分层管理

---

*最后更新: 2025-10-31*

