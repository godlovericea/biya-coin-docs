# 🚀 部署方案

> **核心内容**: Monorepo 应用的部署策略和配置  
> **目标**: 实现独立、高效、可靠的应用部署  
> **更新时间**: 2025-10-31

---

## 📖 目录

1. [部署策略概览](#部署策略概览)
2. [Vercel 部署](#vercel-部署)
3. [自建服务器部署](#自建服务器部署)
4. [CI/CD 配置](#cicd-配置)
5. [环境变量管理](#环境变量管理)
6. [性能优化](#性能优化)

---

## 🎯 部署策略概览

### 独立部署原则

```
Monorepo 单仓库
├── apps/helix       → Vercel Project 1 (helix.biya.com)
├── apps/bridge      → Vercel Project 2 (bridge.biya.com)
└── apps/dex         → Vercel Project 3 (dex.biya.com)

✅ 一个 Git 仓库
✅ 多个 Vercel 项目
✅ 独立域名
✅ 独立构建和部署
```

---

### 部署方案对比

| 特性 | Vercel | 自建服务器 |
|------|--------|-----------|
| **设置难度** | ⭐ 简单 | ⭐⭐⭐ 复杂 |
| **维护成本** | ⭐ 低 | ⭐⭐⭐ 高 |
| **部署速度** | ⭐⭐⭐ 快 | ⭐⭐ 中等 |
| **性能** | ⭐⭐⭐ 优秀 | ⭐⭐ 取决于配置 |
| **成本（小规模）** | ⭐⭐⭐ 免费 | ⭐⭐ 服务器费用 |
| **成本（大规模）** | ⭐⭐ 按量付费 | ⭐⭐⭐ 固定成本 |
| **灵活性** | ⭐⭐ 中等 | ⭐⭐⭐ 完全控制 |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## ☁️ Vercel 部署

### 方案 1：手动部署（推荐用于测试）

#### 步骤 1：安装 Vercel CLI

```bash
npm install -g vercel
```

---

#### 步骤 2：部署 Helix 应用

```bash
cd D:\rwa\biya-coin\apps\helix

# 登录 Vercel
vercel login

# 首次部署（会创建项目）
vercel

# 生产部署
vercel --prod
```

**配置选项**:
- **Set up and deploy**: Yes
- **Which scope**: 选择你的团队或个人账号
- **Link to existing project**: No
- **Project name**: biya-helix
- **Directory**: `apps/helix` 或 `./`（如果已在该目录）
- **Override settings**: Yes
  - **Build Command**: `cd ../.. && npm install && npm run build:helix`
  - **Output Directory**: `.next`
  - **Development Command**: `npm run dev`

---

#### 步骤 3：部署 Bridge 应用

```bash
cd D:\rwa\biya-coin\apps\bridge

# 部署
vercel

# 生产部署
vercel --prod
```

**配置选项**（同上，项目名改为 `biya-bridge`）

---

### 方案 2：通过 Vercel Dashboard 部署（推荐用于生产）

#### 步骤 1：导入项目

1. 访问 [Vercel Dashboard](https://vercel.com/new)
2. 点击 **Add New** → **Project**
3. 导入你的 Git 仓库（GitHub/GitLab/Bitbucket）

---

#### 步骤 2：配置 Helix 项目

**General Settings**:
- **Project Name**: `biya-helix`
- **Framework Preset**: `Next.js`
- **Root Directory**: `apps/helix`

**Build & Development Settings**:
- **Build Command**: `cd ../.. && npm install && npm run build:helix`
- **Output Directory**: `.next`
- **Install Command**: `npm install`
- **Development Command**: `npm run dev`

**Environment Variables**:
```
NEXT_PUBLIC_API_URL=https://api.biya.com
NEXT_PUBLIC_SITE_URL=https://helix.biya.com
```

**Ignored Build Step**:
```bash
node ../../scripts/vercel-ignore-helix.js
```

---

#### 步骤 3：配置 Bridge 项目

重复上述步骤，但使用以下配置：

**General Settings**:
- **Project Name**: `biya-bridge`
- **Root Directory**: `apps/bridge`

**Build Command**:
```bash
cd ../.. && npm install && npm run build:bridge
```

**Environment Variables**:
```
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=your_project_id
NEXT_PUBLIC_ALCHEMY_KEY=your_alchemy_key
NEXT_PUBLIC_ETHEREUM_CHAIN_ID=1
NEXT_PUBLIC_INJECTIVE_CHAIN_ID=injective-1
```

**Ignored Build Step**:
```bash
node ../../scripts/vercel-ignore-bridge.js
```

---

### 步骤 4：创建条件构建脚本

**scripts/vercel-ignore-helix.js**:

```javascript
#!/usr/bin/env node

const { execSync } = require('child_process')

try {
  // 获取变更的文件
  const diff = execSync('git diff HEAD^ HEAD --name-only').toString().trim()
  
  if (!diff) {
    console.log('No changes detected')
    process.exit(0)
  }
  
  const changedFiles = diff.split('\n')
  
  // 检查是否需要构建
  const shouldBuild = changedFiles.some(file => 
    file.startsWith('apps/helix/') || 
    file.startsWith('packages/shared/') ||
    file.startsWith('packages/theme/') ||
    file.startsWith('packages/i18n/')
  )
  
  if (shouldBuild) {
    console.log('✅ Changes detected in Helix or shared packages - Building')
    process.exit(1) // 返回 1 表示需要构建
  } else {
    console.log('⏭️  No relevant changes - Skipping build')
    process.exit(0) // 返回 0 表示跳过构建
  }
} catch (error) {
  console.error('Error checking git diff:', error.message)
  // 出错时默认构建
  process.exit(1)
}
```

**scripts/vercel-ignore-bridge.js**:

```javascript
#!/usr/bin/env node

const { execSync } = require('child_process')

try {
  const diff = execSync('git diff HEAD^ HEAD --name-only').toString().trim()
  
  if (!diff) {
    console.log('No changes detected')
    process.exit(0)
  }
  
  const changedFiles = diff.split('\n')
  
  const shouldBuild = changedFiles.some(file => 
    file.startsWith('apps/bridge/') || 
    file.startsWith('packages/shared/') ||
    file.startsWith('packages/theme/') ||
    file.startsWith('packages/i18n/') ||
    file.startsWith('packages/wallet/')
  )
  
  if (shouldBuild) {
    console.log('✅ Changes detected in Bridge or shared packages - Building')
    process.exit(1)
  } else {
    console.log('⏭️  No relevant changes - Skipping build')
    process.exit(0)
  }
} catch (error) {
  console.error('Error checking git diff:', error.message)
  process.exit(1)
}
```

**别忘了添加执行权限**:
```bash
chmod +x scripts/vercel-ignore-helix.js
chmod +x scripts/vercel-ignore-bridge.js
```

---

### 步骤 5：配置域名

在 Vercel Dashboard 中：

1. 进入项目 → **Settings** → **Domains**
2. 添加自定义域名：
   - Helix: `helix.biya.com`
   - Bridge: `bridge.biya.com`
3. 按照提示配置 DNS（CNAME 或 A 记录）

---

### 步骤 6：配置环境变量

在 Vercel Dashboard 中：

1. 进入项目 → **Settings** → **Environment Variables**
2. 添加变量（注意选择环境：Production/Preview/Development）

**Helix 环境变量**:
```
NEXT_PUBLIC_API_URL
NEXT_PUBLIC_SITE_URL
```

**Bridge 环境变量**:
```
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID
NEXT_PUBLIC_ALCHEMY_KEY
NEXT_PUBLIC_ETHEREUM_CHAIN_ID
NEXT_PUBLIC_INJECTIVE_CHAIN_ID
NEXT_PUBLIC_NETWORK
```

---

## 🖥️ 自建服务器部署

### 方案：Docker + Nginx + PM2

#### 架构图

```
Internet
    ↓
[Nginx] (反向代理)
    ├─→ helix.biya.com:3000 (Helix App)
    ├─→ bridge.biya.com:3001 (Bridge App)
    └─→ dex.biya.com:3002 (DEX App)
```

---

### 步骤 1：准备服务器

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装 PM2
sudo npm install -g pm2

# 安装 Nginx
sudo apt install -y nginx

# 安装 Git
sudo apt install -y git
```

---

### 步骤 2：克隆和构建项目

```bash
# 克隆仓库
cd /var/www
sudo git clone https://github.com/your-org/biya-coin.git
cd biya-coin

# 安装依赖
npm install

# 构建所有应用
npm run build

# 或者只构建特定应用
npm run build:helix
npm run build:bridge
```

---

### 步骤 3：配置 PM2

**创建 ecosystem.config.js**:

```javascript
module.exports = {
  apps: [
    {
      name: 'helix',
      cwd: '/var/www/biya-coin/apps/helix',
      script: 'node_modules/next/dist/bin/next',
      args: 'start -p 3000',
      instances: 2,
      exec_mode: 'cluster',
      env: {
        NODE_ENV: 'production',
        PORT: 3000,
        NEXT_PUBLIC_API_URL: 'https://api.biya.com',
      },
    },
    {
      name: 'bridge',
      cwd: '/var/www/biya-coin/apps/bridge',
      script: 'node_modules/next/dist/bin/next',
      args: 'start -p 3001',
      instances: 2,
      exec_mode: 'cluster',
      env: {
        NODE_ENV: 'production',
        PORT: 3001,
        NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID: 'your_id',
      },
    },
  ],
}
```

**启动应用**:

```bash
cd /var/www/biya-coin

# 启动所有应用
pm2 start ecosystem.config.js

# 保存配置
pm2 save

# 设置开机自启
pm2 startup
```

---

### 步骤 4：配置 Nginx

**创建 /etc/nginx/sites-available/biya-helix**:

```nginx
server {
    listen 80;
    server_name helix.biya.com;

    # 强制 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name helix.biya.com;

    # SSL 证书（使用 Let's Encrypt）
    ssl_certificate /etc/letsencrypt/live/helix.biya.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/helix.biya.com/privkey.pem;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件缓存
    location /_next/static {
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
```

**创建 /etc/nginx/sites-available/biya-bridge**（类似配置，改为 bridge.biya.com 和端口 3001）

**启用站点**:

```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/biya-helix /etc/nginx/sites-enabled/
sudo ln -s /etc/nginx/sites-available/biya-bridge /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

---

### 步骤 5：配置 SSL（Let's Encrypt）

```bash
# 安装 Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d helix.biya.com
sudo certbot --nginx -d bridge.biya.com

# 自动续期
sudo certbot renew --dry-run
```

---

### 步骤 6：配置防火墙

```bash
# 允许 HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 允许 SSH
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable
```

---

## 🔄 CI/CD 配置

### GitHub Actions

**创建 .github/workflows/deploy-helix.yml**:

```yaml
name: Deploy Helix

on:
  push:
    branches:
      - main
    paths:
      - 'apps/helix/**'
      - 'packages/shared/**'
      - 'packages/theme/**'
      - 'packages/i18n/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Helix
        run: npm run build:helix
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID_HELIX }}
          working-directory: ./apps/helix
          vercel-args: '--prod'
```

**创建 .github/workflows/deploy-bridge.yml**（类似配置）

---

### GitLab CI/CD

**创建 .gitlab-ci.yml**:

```yaml
stages:
  - build
  - deploy

variables:
  NODE_VERSION: '18'

# 缓存 node_modules
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - apps/*/node_modules/

# Helix 构建
build:helix:
  stage: build
  image: node:${NODE_VERSION}
  only:
    changes:
      - apps/helix/**/*
      - packages/**/*
  script:
    - npm ci
    - npm run build:helix
  artifacts:
    paths:
      - apps/helix/.next

# Helix 部署
deploy:helix:
  stage: deploy
  only:
    changes:
      - apps/helix/**/*
      - packages/**/*
  dependencies:
    - build:helix
  script:
    - npm install -g vercel
    - vercel --token $VERCEL_TOKEN --prod --cwd apps/helix

# Bridge 构建和部署（类似配置）
```

---

## 🔐 环境变量管理

### 开发环境

**apps/helix/.env.local**:
```bash
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**apps/bridge/.env.local**:
```bash
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=dev_project_id
NEXT_PUBLIC_ALCHEMY_KEY=dev_alchemy_key
NEXT_PUBLIC_ETHEREUM_CHAIN_ID=11155111  # Sepolia
NEXT_PUBLIC_INJECTIVE_CHAIN_ID=injective-888
```

---

### 生产环境

**方式 1：Vercel Dashboard**
- Settings → Environment Variables → Add

**方式 2：Vercel CLI**
```bash
vercel env add NEXT_PUBLIC_API_URL production
vercel env add NEXT_PUBLIC_SITE_URL production
```

**方式 3：自建服务器（使用 .env 文件）**
```bash
# /var/www/biya-coin/apps/helix/.env.production
NEXT_PUBLIC_API_URL=https://api.biya.com
NEXT_PUBLIC_SITE_URL=https://helix.biya.com
```

---

### 环境变量最佳实践

1. **使用 NEXT_PUBLIC_ 前缀**：客户端可访问的变量
2. **敏感信息不使用前缀**：服务端专用
3. **版本控制**：.env.example 提交到 Git，.env.local 不提交
4. **分环境管理**：development/preview/production

---

## ⚡ 性能优化

### 1. 构建优化

**next.config.ts**:

```typescript
const nextConfig: NextConfig = {
  // 压缩
  compress: true,
  
  // 图片优化
  images: {
    domains: ['your-cdn.com'],
    formats: ['image/avif', 'image/webp'],
  },
  
  // 实验性优化
  experimental: {
    optimizePackageImports: [
      '@biya/shared',
      '@biya/theme',
      'lucide-react',
    ],
  },
  
  // 生产 Source Map（可选）
  productionBrowserSourceMaps: false,
}
```

---

### 2. 缓存策略

**Vercel 自动缓存**:
- `_next/static/*` → 永久缓存
- 页面 → 根据 ISR 配置

**自建服务器 Nginx 缓存**:
```nginx
location /_next/static {
    proxy_pass http://localhost:3000;
    add_header Cache-Control "public, max-age=31536000, immutable";
}

location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

---

### 3. CDN 配置

**Vercel**:
- 自动全球 CDN

**自建服务器**:
- 使用 Cloudflare、AWS CloudFront 等

---

### 4. 监控和分析

**Vercel Analytics**:
```typescript
// app/layout.tsx
import { Analytics } from '@vercel/analytics/react'

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

**自建监控**:
- PM2 Plus
- Grafana + Prometheus
- Sentry（错误追踪）

---

## 📋 部署清单

### Vercel 部署清单
- [ ] 创建 Vercel 项目（Helix、Bridge）
- [ ] 配置 Root Directory
- [ ] 配置 Build Command
- [ ] 添加环境变量
- [ ] 配置 Ignored Build Step
- [ ] 添加自定义域名
- [ ] 配置 DNS
- [ ] 测试部署

### 自建服务器清单
- [ ] 服务器配置（Node.js、Nginx、PM2）
- [ ] 克隆代码仓库
- [ ] 安装依赖
- [ ] 构建应用
- [ ] 配置 PM2
- [ ] 配置 Nginx
- [ ] 配置 SSL 证书
- [ ] 配置防火墙
- [ ] 设置自动部署（CI/CD）
- [ ] 配置监控

---

## 🚀 下一步

部署方案已明确，继续阅读：

**→ [08-FAQ最佳实践.md](./08-FAQ最佳实践.md)**

了解常见问题和最佳实践。

---

## 📚 本章小结

### 部署选项

1. **Vercel**（推荐）
   - 简单快速
   - 自动优化
   - 全球 CDN
   - 适合中小型项目

2. **自建服务器**
   - 完全控制
   - 固定成本
   - 需要维护
   - 适合大型项目或特殊需求

### 关键配置

1. **独立部署**：每个应用独立配置
2. **条件构建**：只构建变更的应用
3. **环境变量**：分环境管理
4. **CI/CD**：自动化部署流程
5. **性能优化**：缓存、CDN、监控

---

*最后更新: 2025-10-31*

