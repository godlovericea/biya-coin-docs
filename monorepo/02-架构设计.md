# 🏗️ Monorepo 架构设计

> **核心概念**: Workspace、Package、共享与独立  
> **目标**: 理解 Monorepo 架构原理和设计模式  
> **更新时间**: 2025-10-31

---

## 📖 目录

1. [什么是 Monorepo](#什么是-monorepo)
2. [核心概念](#核心概念)
3. [架构原理](#架构原理)
4. [代码组织策略](#代码组织策略)
5. [模块划分原则](#模块划分原则)
6. [依赖管理](#依赖管理)

---

## 🎯 什么是 Monorepo

### 定义

**Monorepo（单一仓库）**: 在一个 Git 仓库中管理多个相关项目的代码组织方式。

```
传统方式（Multi-repo）:
├── bridge-repo/          (独立仓库)
│   └── src/
├── dex-repo/             (独立仓库)
│   └── src/
└── helix-repo/           (独立仓库)
    └── src/

Monorepo 方式:
biya-coin/                (单一仓库)
├── apps/
│   ├── bridge/           (应用1)
│   ├── dex/              (应用2)
│   └── helix/            (应用3)
└── packages/
    └── shared/           (共享代码)
```

---

### 关键特点

#### 1. 单一仓库，多个项目

```
✅ 所有代码在一个仓库
✅ 统一的版本控制
✅ 统一的依赖管理
✅ 统一的工具链
```

#### 2. 代码共享

```typescript
// packages/shared/components/Button.tsx
export function Button({ children, onClick }) {
  return <button onClick={onClick}>{children}</button>
}

// apps/bridge/app/page.tsx
import { Button } from '@biya/shared'  // ✅ 直接引用

// apps/dex/app/page.tsx
import { Button } from '@biya/shared'  // ✅ 直接引用
```

#### 3. 独立部署

```
虽然代码在一起，但部署是独立的:

apps/bridge/  → bridge.biya.com   (独立部署)
apps/dex/     → dex.biya.com      (独立部署)
apps/helix/   → biya.com          (独立部署)

修改 Bridge 代码:
- 只重新部署 Bridge ✅
- DEX 和 Helix 不受影响 ✅
```

---

### Monorepo vs Multi-repo

| 维度 | Monorepo | Multi-repo |
|------|----------|-----------|
| **仓库数量** | 1 个 | 多个 |
| **代码可见性** | ✅ 全部可见 | ❌ 各自独立 |
| **代码复用** | ✅ 直接引用 | ❌ 需发布 npm 包 |
| **版本管理** | ✅ 统一 | ❌ 各自管理 |
| **依赖管理** | ✅ 统一 | ❌ 各自管理 |
| **CI/CD** | ✅ 统一配置 | ❌ 各自配置 |
| **代码跳转** | ✅ 直接跳转 | ❌ 无法跳转 |
| **类型检查** | ✅ 完整 | ❌ 受限 |
| **独立部署** | ✅ 支持 | ✅ 支持 |

**结论**: Monorepo 在代码复用和开发体验上更优 ✅

---

## 🧩 核心概念

### 1. Workspace（工作区）

**定义**: 包含多个 package 的集合。

```json
// package.json（根目录）
{
  "workspaces": [
    "apps/*",      // 所有应用
    "packages/*"   // 所有共享包
  ]
}
```

**作用**:
- ✅ 统一依赖安装
- ✅ 本地包引用
- ✅ 共享 node_modules

---

### 2. Package（包）

**定义**: Monorepo 中的独立项目单元。

```
biya-coin/
├── apps/bridge/           ← Package（应用包）
│   └── package.json
├── apps/dex/              ← Package（应用包）
│   └── package.json
└── packages/shared/       ← Package（共享包）
    └── package.json
```

**每个 Package**:
- 有自己的 `package.json`
- 可以有自己的依赖
- 可以被其他 Package 引用

---

### 3. 应用包 vs 共享包

#### 应用包（apps/）

```
特点:
- 可以独立运行
- 可以独立部署
- 有自己的入口
- 通常是 Next.js 应用

示例:
apps/bridge/   → 跨链桥应用
apps/dex/      → DEX 交易应用
apps/helix/    → 主站应用
```

#### 共享包（packages/）

```
特点:
- 不能独立运行
- 被应用包引用
- 提供可复用代码
- 通常是工具、组件、类型

示例:
packages/shared/   → 通用组件和工具
packages/theme/    → 主题管理
packages/i18n/     → 国际化
```

---

### 4. 本地引用

**核心机制**: 使用 `workspace:*` 协议引用本地包。

```json
// apps/bridge/package.json
{
  "name": "@biya/bridge",
  "dependencies": {
    "@biya/shared": "workspace:*",   // ← 引用本地包
    "@biya/theme": "workspace:*",
    "next": "^15.5.4"                // ← 引用 npm 包
  }
}
```

**效果**:
```typescript
// apps/bridge/app/page.tsx
import { Button } from '@biya/shared'  // ✅ 直接引用，自动解析
```

---

## 🔧 架构原理

### 1. 依赖提升（Hoisting）

**原理**: 将公共依赖提升到根 `node_modules`。

```
传统方式（没有 Workspace）:
apps/bridge/node_modules/
  ├── react/
  ├── next/
  └── ...
apps/dex/node_modules/
  ├── react/          ← 重复
  ├── next/           ← 重复
  └── ...

Workspace 方式:
node_modules/         ← 共享依赖
  ├── react/          ✅ 只有一份
  ├── next/           ✅ 只有一份
  └── @biya/          ← 本地包软链接
      ├── shared -> ../../packages/shared
      ├── theme -> ../../packages/theme
      └── i18n -> ../../packages/i18n
apps/bridge/node_modules/
  └── (应用特定依赖)
apps/dex/node_modules/
  └── (应用特定依赖)
```

**优势**:
- ✅ 节省磁盘空间
- ✅ 加快安装速度
- ✅ 确保版本一致

---

### 2. 符号链接（Symlinks）

**原理**: 使用软链接连接本地包。

```bash
# 查看软链接
ls -la node_modules/@biya/

lrwxrwxrwx  shared -> ../../packages/shared
lrwxrwxrwx  theme -> ../../packages/theme
lrwxrwxrwx  i18n -> ../../packages/i18n
```

**效果**:
```typescript
// 在 apps/bridge 中
import { Button } from '@biya/shared'

// 实际解析为
import { Button } from '../../packages/shared'

// ✅ 直接访问源码
// ✅ 修改立即生效
// ✅ 类型检查完整
```

---

### 3. 包解析流程

**查找顺序**:

```
1. 检查是否是本地包
   @biya/shared → node_modules/@biya/shared (软链接)
   → ../../packages/shared ✅

2. 如果不是本地包，查找 node_modules
   react → node_modules/react (根目录)
   或 node_modules/react (应用目录)

3. 如果都找不到，报错
   ❌ Cannot find module
```

---

## 📦 代码组织策略

### 1. 目录结构设计

**基础结构**:

```
biya-coin/
├── package.json              # 根配置
├── pnpm-workspace.yaml       # Workspace 配置
├── tsconfig.json             # TS 根配置
├── .gitignore
│
├── node_modules/             # 共享依赖
│
├── apps/                     # 应用目录
│   ├── bridge/
│   ├── dex/
│   └── helix/
│
├── packages/                 # 共享包目录
│   ├── shared/
│   ├── theme/
│   ├── i18n/
│   ├── wallet/
│   └── auth/
│
├── scripts/                  # 构建脚本
│   ├── build-all.sh
│   └── deploy.sh
│
└── docs/                     # 文档
    └── monorepo/
```

---

### 2. Package 命名规范

**推荐使用作用域（Scoped Packages）**:

```json
// ✅ 好的命名
{
  "name": "@biya/bridge",      // 应用包
  "name": "@biya/shared",      // 共享包
  "name": "@biya/theme"        // 共享包
}

// ❌ 不好的命名
{
  "name": "bridge",            // 可能与 npm 包冲突
  "name": "shared",            // 太泛化
  "name": "utils"              // 不清晰
}
```

**命名规则**:
- 应用包: `@biya/[app-name]`
- 共享包: `@biya/[feature-name]`
- 全部小写
- 使用连字符分隔

---

### 3. 文件组织原则

#### 应用包结构（apps/bridge/）

```
apps/bridge/
├── package.json              # 包配置
├── next.config.ts            # Next.js 配置
├── tsconfig.json             # TS 配置
├── tailwind.config.ts        # Tailwind 配置
│
├── app/                      # Next.js App Router
│   ├── [locale]/             # 国际化路由
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── bridge/
│   │       └── page.tsx
│   └── layout.tsx
│
├── components/               # Bridge 特有组件
│   └── bridge/
│       ├── BridgeForm.tsx
│       └── BridgeHistory.tsx
│
├── lib/                      # Bridge 业务逻辑
│   └── bridge/
│       ├── services/
│       ├── utils/
│       └── types/
│
├── store/                    # Bridge 特定状态
│   ├── bridge-store.ts
│   └── tx-history-store.ts
│
├── messages/                 # Bridge 特定翻译
│   ├── en.json
│   └── zh.json
│
├── public/                   # 静态资源
│   └── images/
│
└── styles/
    └── globals.css
```

---

#### 共享包结构（packages/shared/）

```
packages/shared/
├── package.json              # 包配置
├── tsconfig.json             # TS 配置
│
├── components/               # 共享组件
│   ├── index.ts              # 统一导出
│   ├── ui/                   # 基础 UI
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   └── Card.tsx
│   └── layout/               # 布局组件
│       ├── Header.tsx
│       └── Footer.tsx
│
├── hooks/                    # 共享 Hooks
│   ├── index.ts
│   ├── useDebounce.ts
│   └── useMediaQuery.ts
│
├── utils/                    # 工具函数
│   ├── index.ts
│   ├── format.ts
│   └── validation.ts
│
├── types/                    # 共享类型
│   ├── index.ts
│   └── common.ts
│
└── index.ts                  # 根导出
```

**根导出文件**:
```typescript
// packages/shared/index.ts
export * from './components'
export * from './hooks'
export * from './utils'
export * from './types'
```

---

## 🎨 模块划分原则

### 1. 共享 vs 独立

**决策流程**:

```
创建新功能时，问自己：

1️⃣ 这个功能会被多个应用使用吗？
   ↓ YES                    ↓ NO
   放在 packages/           放在 apps/[app]/

2️⃣ 这个功能是否依赖特定应用？
   ↓ YES                    ↓ NO
   放在 apps/[app]/         放在 packages/

3️⃣ 这个功能是否会频繁变化？
   ↓ YES                    ↓ NO
   放在 apps/[app]/         放在 packages/
```

---

### 2. 分层策略

**三层架构**:

```
┌─────────────────────────────────┐
│  应用层（apps/）                 │  ← 业务逻辑、页面、路由
│  - Bridge 业务                   │
│  - DEX 业务                      │
│  - Helix 业务                    │
└─────────────────────────────────┘
           ↓ 依赖
┌─────────────────────────────────┐
│  领域层（packages/domain/）      │  ← 领域特定逻辑
│  - wallet (钱包)                 │
│  - trading (交易)                │
│  - bridge (桥接)                 │
└─────────────────────────────────┘
           ↓ 依赖
┌─────────────────────────────────┐
│  基础层（packages/shared/）      │  ← 通用组件、工具
│  - UI 组件                       │
│  - Hooks                         │
│  - 工具函数                      │
└─────────────────────────────────┘
```

---

### 3. 模块粒度

#### 粗粒度（推荐）

```
packages/
├── shared/                   ✅ 一个大包
│   ├── components/
│   ├── hooks/
│   └── utils/
├── theme/                    ✅ 按功能分包
└── i18n/                     ✅ 按功能分包
```

**优点**:
- ✅ 管理简单
- ✅ 依赖清晰
- ✅ 导入方便

---

#### 细粒度（不推荐）

```
packages/
├── button/                   ❌ 过于细分
├── input/                    ❌ 过于细分
├── card/                     ❌ 过于细分
├── use-debounce/             ❌ 过于细分
└── use-media-query/          ❌ 过于细分
```

**缺点**:
- ❌ 管理复杂
- ❌ 依赖混乱
- ❌ 导入繁琐

---

## 🔗 依赖管理

### 1. 依赖类型

#### 外部依赖

```json
// apps/bridge/package.json
{
  "dependencies": {
    "next": "^15.5.4",           // npm 包
    "react": "^19.1.0",          // npm 包
    "ethers": "^6.15.0"          // npm 包
  }
}
```

#### 内部依赖

```json
// apps/bridge/package.json
{
  "dependencies": {
    "@biya/shared": "workspace:*",   // 本地包
    "@biya/theme": "workspace:*",    // 本地包
    "@biya/wallet": "workspace:*"    // 本地包
  }
}
```

---

### 2. 依赖安装流程

**命令**:
```bash
# pnpm
pnpm install

# npm
npm install
```

**执行流程**:
```
1. 读取根 package.json
   ↓
2. 读取所有 workspace 的 package.json
   ↓
3. 解析所有依赖（外部 + 内部）
   ↓
4. 安装外部依赖到根 node_modules
   ↓
5. 创建本地包软链接
   ↓
6. 完成 ✅
```

---

### 3. 版本管理策略

#### 统一版本（推荐）

```json
// 根 package.json
{
  "dependencies": {
    "react": "^19.1.0",
    "next": "^15.5.4"
  }
}

// apps/bridge/package.json
{
  "dependencies": {
    "react": "^19.1.0",    // ✅ 版本一致
    "next": "^15.5.4"      // ✅ 版本一致
  }
}
```

**优点**:
- ✅ 避免版本冲突
- ✅ 统一升级
- ✅ 减少依赖体积

---

#### 独立版本（不推荐）

```json
// apps/bridge/package.json
{
  "dependencies": {
    "react": "^19.1.0"     // Bridge 用 19.1
  }
}

// apps/dex/package.json
{
  "dependencies": {
    "react": "^18.2.0"     // DEX 用 18.2  ❌ 版本不一致
  }
}
```

**缺点**:
- ❌ 版本冲突风险
- ❌ 增加依赖体积
- ❌ 维护困难

---

### 4. 依赖提升规则

**自动提升**:
```
如果多个包依赖相同版本 → 提升到根 node_modules

apps/bridge:  react@19.1.0
apps/dex:     react@19.1.0
apps/helix:   react@19.1.0
              ↓
node_modules/react@19.1.0  ✅ 提升到根
```

**不提升**:
```
如果版本不同 → 保留在各自的 node_modules

apps/bridge:  lodash@4.17.21
apps/dex:     lodash@4.17.20
              ↓
apps/bridge/node_modules/lodash@4.17.21
apps/dex/node_modules/lodash@4.17.20
```

---

## 🎯 架构设计最佳实践

### 1. 保持简单

```
✅ 从简单开始
   - 先创建 apps/ 和 packages/shared
   - 需要时再拆分更多共享包

❌ 避免过度设计
   - 不要一开始就创建 20 个共享包
   - 不要过早优化
```

---

### 2. 明确边界

```
✅ 清晰的职责划分
   apps/        → 应用特定代码
   packages/    → 可复用代码

✅ 避免循环依赖
   apps/bridge 不应该依赖 apps/dex
   packages/shared 不应该依赖 apps/bridge
```

---

### 3. 渐进式迁移

```
阶段 1: 基础结构
  └── 创建 apps/ 和 packages/

阶段 2: 提取共享代码
  └── 将公共组件移到 packages/shared

阶段 3: 功能包
  └── 创建 theme、i18n 等功能包

阶段 4: 优化
  └── 按需拆分、性能优化
```

---

### 4. 文档和约定

```
✅ 维护文档
   - README.md 说明项目结构
   - 包的使用说明

✅ 统一约定
   - 命名规范
   - 文件组织
   - 导入导出规范
```

---

## 📊 架构决策记录

### ADR 示例

**ADR-001: 使用 Monorepo 架构**

```
决策:
  使用 Monorepo 而不是 Multi-repo

理由:
  1. 代码复用更方便
  2. 统一的依赖管理
  3. 更好的开发体验
  4. 完整的类型检查

权衡:
  优势: 代码共享、统一管理
  劣势: 初始配置复杂

结果:
  ✅ 采用 Monorepo
```

---

## 🎯 实战：biya-helix-app 迁移到 Monorepo

### 当前结构（biya-helix-app）

```
biya-helix-app/
├── app/
│   ├── [locale]/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── about/
│   │   ├── bridge/              ← 桥接页面
│   │   ├── hello/
│   │   └── zustand-demo/
│   └── api/
│
├── components/
│   ├── bridge/                  ← 桥接组件
│   │   ├── Bridge.tsx
│   │   ├── BridgeForm.tsx
│   │   ├── BridgeFromV2.tsx
│   │   └── common/
│   ├── Button.tsx               ← 通用组件
│   ├── Card.tsx
│   ├── Header.tsx
│   └── wallet/                  ← 钱包组件
│
├── context/
│   ├── bridge/                  ← 桥接 Context
│   └── ThemeContext.tsx         ← 主题 Context
│
├── lib/
│   ├── bridge/                  ← 桥接业务逻辑
│   │   ├── constants/
│   │   ├── contracts/
│   │   ├── services/
│   │   ├── utils/
│   │   └── wallet/
│   ├── grpc/
│   └── store/
│
├── messages/                    ← 多语言
│   ├── en.json
│   ├── zh.json
│   ├── ja.json
│   ├── ko.json
│   └── es.json
│
├── i18n/
│   ├── config.ts
│   └── request.ts
│
├── public/
│   └── images/
│
├── styles/
│   ├── globals.css
│   └── components.css
│
├── vendor/                      ← 本地化依赖
│   └── injective/
│
├── package.json
├── next.config.ts
├── tsconfig.json
└── tailwind.config.js
```

---

### 目标结构（Monorepo）

```
biya-coin/                          # Monorepo 根目录
│
├── 📄 package.json                 # 根配置（Workspaces）
├── 📄 pnpm-workspace.yaml          # pnpm 配置
├── 📄 tsconfig.json                # TS 根配置
├── 📄 .gitignore
├── 📄 README.md
│
├── 📁 node_modules/                # 共享依赖
│   ├── react/
│   ├── next/
│   └── @biya/                      # 本地包软链接
│       ├── shared -> ../../packages/shared
│       ├── theme -> ../../packages/theme
│       ├── i18n -> ../../packages/i18n
│       ├── wallet -> ../../packages/wallet
│       └── auth -> ../../packages/auth
│
├── 📁 apps/                        # 应用目录
│   │
│   ├── 📁 helix/                   # 主站应用（从 biya-helix-app 迁移）
│   │   ├── 📄 package.json
│   │   ├── 📄 next.config.ts
│   │   ├── 📄 tsconfig.json
│   │   ├── 📄 tailwind.config.js
│   │   ├── 📄 vercel.json
│   │   │
│   │   ├── 📁 app/
│   │   │   ├── 📁 [locale]/
│   │   │   │   ├── 📄 layout.tsx
│   │   │   │   ├── 📄 page.tsx
│   │   │   │   ├── 📁 about/
│   │   │   │   ├── 📁 hello/
│   │   │   │   └── 📁 api-test/
│   │   │   └── 📁 api/
│   │   │
│   │   ├── 📁 components/         # Helix 特有组件
│   │   │   ├── 📁 home/
│   │   │   ├── 📁 about/
│   │   │   └── 📄 TradingCard.tsx
│   │   │
│   │   ├── 📁 lib/                # Helix 业务逻辑
│   │   │   ├── 📁 grpc/
│   │   │   └── 📁 utils/
│   │   │
│   │   ├── 📁 messages/           # Helix 特定翻译
│   │   │   ├── 📄 en.json
│   │   │   └── 📄 zh.json
│   │   │
│   │   ├── 📁 public/
│   │   │   └── 📁 images/
│   │   │
│   │   ├── 📁 styles/
│   │   │   └── 📄 globals.css
│   │   │
│   │   └── 📁 i18n/
│   │       └── 📄 request.ts
│   │
│   ├── 📁 bridge/                  # 跨链桥应用（独立出来）
│   │   ├── 📄 package.json
│   │   ├── 📄 next.config.ts
│   │   ├── 📄 tsconfig.json
│   │   ├── 📄 tailwind.config.js
│   │   ├── 📄 vercel.json
│   │   │
│   │   ├── 📁 app/
│   │   │   ├── 📁 [locale]/
│   │   │   │   ├── 📄 layout.tsx
│   │   │   │   ├── 📄 page.tsx        # Bridge 首页
│   │   │   │   └── 📁 bridge/
│   │   │   │       └── 📄 page.tsx    # 桥接页面
│   │   │   └── 📄 layout.tsx
│   │   │
│   │   ├── 📁 components/              # Bridge 特有组件
│   │   │   └── 📁 bridge/
│   │   │       ├── 📄 Bridge.tsx
│   │   │       ├── 📄 BridgeForm.tsx
│   │   │       ├── 📄 BridgeFromV2.tsx
│   │   │       └── 📁 common/
│   │   │           ├── 📄 CurrencyInput.tsx
│   │   │           └── 📄 Spinner.tsx
│   │   │
│   │   ├── 📁 context/                 # Bridge Context
│   │   │   └── 📁 bridge/
│   │   │       ├── 📄 BridgeProviders.tsx
│   │   │       ├── 📄 WalletProvider.tsx
│   │   │       ├── 📄 AccountProvider.tsx
│   │   │       ├── 📄 PeggyProvider.tsx
│   │   │       ├── 📄 AxelarProvider.tsx
│   │   │       ├── 📄 TokenProvider.tsx
│   │   │       └── 📄 EventProvider.tsx
│   │   │
│   │   ├── 📁 lib/                     # Bridge 业务逻辑
│   │   │   └── 📁 bridge/
│   │   │       ├── 📁 constants/
│   │   │       ├── 📁 contracts/
│   │   │       │   ├── 📁 abi/
│   │   │       │   ├── 📄 BaseContract.ts
│   │   │       │   ├── 📄 Erc20Contract.ts
│   │   │       │   └── 📄 Peggy.ts
│   │   │       ├── 📁 services/
│   │   │       │   ├── 📄 service.ts
│   │   │       │   ├── 📄 web3Client.ts
│   │   │       │   ├── 📄 web3composer.ts
│   │   │       │   └── 📄 ethGasPrice.ts
│   │   │       ├── 📁 utils/
│   │   │       ├── 📁 types/
│   │   │       ├── 📁 data/
│   │   │       ├── 📁 hooks/
│   │   │       ├── 📁 config/
│   │   │       └── 📁 wallet/
│   │   │
│   │   ├── 📁 store/                   # Bridge 特定状态
│   │   │   ├── 📄 bridge-store.ts
│   │   │   └── 📄 transaction-store.ts
│   │   │
│   │   ├── 📁 messages/                # Bridge 特定翻译
│   │   │   ├── 📄 en.json
│   │   │   └── 📄 zh.json
│   │   │
│   │   ├── 📁 public/
│   │   │   └── 📁 images/
│   │   │       └── 📁 chains/
│   │   │           ├── 📄 ethereum.svg
│   │   │           ├── 📄 bnb.svg
│   │   │           └── 📄 injective.svg
│   │   │
│   │   └── 📁 i18n/
│   │       └── 📄 request.ts
│   │
│   └── 📁 dex/                         # DEX 应用（待创建）
│       ├── 📄 package.json
│       ├── 📄 next.config.ts
│       └── ...
│
├── 📁 packages/                        # 共享包目录
│   │
│   ├── 📁 shared/                      # 基础共享包 ⭐
│   │   ├── 📄 package.json
│   │   ├── 📄 tsconfig.json
│   │   │
│   │   ├── 📁 components/              # 共享 UI 组件
│   │   │   ├── 📄 index.ts
│   │   │   ├── 📁 ui/
│   │   │   │   ├── 📄 Button.tsx       # 从 biya-helix-app 迁移
│   │   │   │   ├── 📄 Card.tsx         # 从 biya-helix-app 迁移
│   │   │   │   ├── 📄 Input.tsx
│   │   │   │   ├── 📄 Modal.tsx
│   │   │   │   └── 📄 Loading.tsx
│   │   │   └── 📁 layout/
│   │   │       ├── 📄 Header.tsx       # 从 biya-helix-app 迁移
│   │   │       ├── 📄 Footer.tsx
│   │   │       └── 📄 Container.tsx
│   │   │
│   │   ├── 📁 hooks/                   # 共享 Hooks
│   │   │   ├── 📄 index.ts
│   │   │   ├── 📄 useDebounce.ts
│   │   │   ├── 📄 useMediaQuery.ts
│   │   │   └── 📄 useLocalStorage.ts
│   │   │
│   │   ├── 📁 utils/                   # 工具函数
│   │   │   ├── 📄 index.ts
│   │   │   ├── 📄 format.ts
│   │   │   ├── 📄 validation.ts
│   │   │   └── 📄 cn.ts
│   │   │
│   │   ├── 📁 types/                   # 共享类型
│   │   │   ├── 📄 index.ts
│   │   │   └── 📄 common.ts
│   │   │
│   │   └── 📄 index.ts                 # 统一导出
│   │
│   ├── 📁 theme/                       # 主题管理包 ⭐
│   │   ├── 📄 package.json
│   │   │
│   │   ├── 📁 store/                   # 主题状态
│   │   │   └── 📄 theme-store.ts       # 从 ThemeContext.tsx 重构
│   │   │
│   │   ├── 📁 hooks/
│   │   │   └── 📄 useTheme.ts
│   │   │
│   │   ├── 📁 components/
│   │   │   ├── 📄 ThemeProvider.tsx
│   │   │   ├── 📄 ThemeToggle.tsx      # 从 SelectTheme.tsx 重构
│   │   │   └── 📄 ThemeScript.tsx
│   │   │
│   │   ├── 📁 config/
│   │   │   ├── 📄 themes.ts
│   │   │   └── 📄 colors.ts            # 从 tailwind-colors.ts 迁移
│   │   │
│   │   └── 📄 index.ts
│   │
│   ├── 📁 i18n/                        # 国际化包 ⭐
│   │   ├── 📄 package.json
│   │   │
│   │   ├── 📁 locales/                 # 分层翻译
│   │   │   ├── 📁 common/              # 通用翻译
│   │   │   │   ├── 📄 en.json
│   │   │   │   ├── 📄 zh.json
│   │   │   │   ├── 📄 ja.json
│   │   │   │   ├── 📄 ko.json
│   │   │   │   └── 📄 es.json
│   │   │   └── 📁 domain/              # 领域翻译
│   │   │       ├── 📁 wallet/
│   │   │       │   ├── 📄 en.json
│   │   │       │   └── 📄 zh.json
│   │   │       ├── 📁 trading/
│   │   │       │   ├── 📄 en.json
│   │   │       │   └── 📄 zh.json
│   │   │       └── 📁 bridge/
│   │   │           ├── 📄 en.json
│   │   │           └── 📄 zh.json
│   │   │
│   │   ├── 📁 hooks/
│   │   │   └── 📄 useTranslation.ts
│   │   │
│   │   ├── 📁 utils/
│   │   │   └── 📄 merge.ts             # 翻译合并工具
│   │   │
│   │   ├── 📁 config/
│   │   │   ├── 📄 locales.ts           # 从 config/locales.ts 迁移
│   │   │   └── 📄 next-intl.ts
│   │   │
│   │   └── 📄 index.ts
│   │
│   ├── 📁 wallet/                      # 钱包管理包 ⭐
│   │   ├── 📄 package.json
│   │   │
│   │   ├── 📁 store/
│   │   │   └── 📄 wallet-store.ts      # Zustand Store
│   │   │
│   │   ├── 📁 hooks/
│   │   │   ├── 📄 useWallet.ts
│   │   │   └── 📄 useBalance.ts
│   │   │
│   │   ├── 📁 components/
│   │   │   ├── 📄 WalletButton.tsx
│   │   │   ├── 📄 WalletModal.tsx      # 从 WalletConnectModal 重构
│   │   │   └── 📄 WalletIcon.tsx       # 从 biya-helix-app 迁移
│   │   │
│   │   ├── 📁 services/
│   │   │   └── 📄 wallet-service.ts    # 从 lib/wallet-service.ts 迁移
│   │   │
│   │   └── 📄 index.ts
│   │
│   ├── 📁 auth/                        # 认证管理包 ⭐
│   │   ├── 📄 package.json
│   │   │
│   │   ├── 📁 store/
│   │   │   └── 📄 auth-store.ts
│   │   │
│   │   ├── 📁 hooks/
│   │   │   └── 📄 useAuth.ts
│   │   │
│   │   └── 📄 index.ts
│   │
│   └── 📁 config/                      # 配置包
│       ├── 📄 package.json
│       ├── 📄 eslint-config.js
│       ├── 📄 prettier-config.js
│       ├── 📄 tsconfig.base.json
│       └── 📄 tailwind-config.js
│
├── 📁 vendor/                          # 本地化依赖（保留）
│   └── 📁 injective/
│       ├── 📁 sdk-ts/
│       ├── 📁 core-proto-ts/
│       ├── 📁 wallet-strategy/
│       └── 📄 MODIFICATIONS.md
│
├── 📁 scripts/                         # 构建和部署脚本
│   ├── 📄 check-should-deploy.js
│   ├── 📄 build-all.sh
│   └── 📄 deploy.sh
│
├── 📁 docs/                            # 文档
│   ├── 📁 monorepo/                    # Monorepo 文档
│   └── 📁 bridge/                      # 桥接文档
│
├── 📁 .github/                         # CI/CD
│   └── 📁 workflows/
│       ├── 📄 deploy-bridge.yml
│       ├── 📄 deploy-dex.yml
│       └── 📄 deploy-helix.yml
│
└── 📁 nginx/                           # Nginx 配置（自建服务器）
    └── 📁 conf.d/
```

---

### 迁移映射表

| 原路径（biya-helix-app） | 目标路径（Monorepo） | 说明 |
|------------------------|-------------------|------|
| **应用代码** | | |
| `app/` | `apps/helix/app/` | 主站应用 |
| `app/bridge/` | `apps/bridge/app/bridge/` | 桥接独立出来 |
| `components/bridge/` | `apps/bridge/components/bridge/` | Bridge 组件 |
| `context/bridge/` | `apps/bridge/context/bridge/` | Bridge Context |
| `lib/bridge/` | `apps/bridge/lib/bridge/` | Bridge 逻辑 |
| **共享代码** | | |
| `components/Button.tsx` | `packages/shared/components/ui/Button.tsx` | 共享组件 |
| `components/Card.tsx` | `packages/shared/components/ui/Card.tsx` | 共享组件 |
| `components/Header.tsx` | `packages/shared/components/layout/Header.tsx` | 共享组件 |
| `context/ThemeContext.tsx` | `packages/theme/store/theme-store.ts` | 重构为 Zustand |
| `components/SelectTheme.tsx` | `packages/theme/components/ThemeToggle.tsx` | 主题切换 |
| `components/wallet/` | `packages/wallet/components/` | 钱包组件 |
| `lib/wallet-service.ts` | `packages/wallet/services/wallet-service.ts` | 钱包服务 |
| **多语言** | | |
| `messages/en.json` | `packages/i18n/locales/common/en.json` + `apps/*/messages/en.json` | 分层翻译 |
| `i18n/config.ts` | `packages/i18n/config/locales.ts` | i18n 配置 |
| `config/locales.ts` | `packages/i18n/config/locales.ts` | 语言配置 |
| **配置文件** | | |
| `tailwind.config.js` | `packages/config/tailwind-config.js` + `apps/*/tailwind.config.js` | 共享 + 定制 |
| `tsconfig.json` | `tsconfig.json` + `apps/*/tsconfig.json` | 根配置 + 应用配置 |
| **其他** | | |
| `vendor/` | `vendor/` | 保持不变 |
| `public/` | `apps/*/public/` | 各应用独立 |
| `styles/` | `apps/*/styles/` | 各应用独立 |

---

### 多语言架构详解

#### 分层翻译策略

```
完整的翻译链:

1. 通用层（packages/i18n/locales/common/）
   ├── en.json
   │   ├── "common": { "button": "Submit", "cancel": "Cancel" }
   │   └── "language": { "en": "English", "zh": "中文" }
   └── zh.json
       └── ... (中文翻译)

2. 领域层（packages/i18n/locales/domain/）
   ├── wallet/
   │   ├── en.json
   │   │   └── "wallet": { "connect": "Connect Wallet" }
   │   └── zh.json
   │       └── "wallet": { "connect": "连接钱包" }
   │
   ├── trading/
   │   ├── en.json
   │   └── zh.json
   │
   └── bridge/
       ├── en.json
       │   └── "bridge": { "transfer": "Transfer", "from": "From" }
       └── zh.json
           └── "bridge": { "transfer": "转账", "from": "从" }

3. 应用层（apps/*/messages/）
   ├── apps/bridge/messages/
   │   ├── en.json
   │   │   └── "bridge": { "page_title": "Cross-Chain Bridge" }
   │   └── zh.json
   │       └── "bridge": { "page_title": "跨链桥" }
   │
   └── apps/helix/messages/
       ├── en.json
       └── zh.json
```

#### 翻译合并机制

```typescript
// apps/bridge/i18n/request.ts
import { getRequestConfig } from 'next-intl/server'
import { mergeTranslations } from '@biya/i18n'

export default getRequestConfig(async ({ locale }) => {
  // 按顺序合并：common → domain(wallet) → domain(bridge) → app(bridge)
  const messages = mergeTranslations(locale, [
    'common',        // 1. 通用翻译
    'wallet',        // 2. 钱包领域翻译
    'bridge',        // 3. 桥接领域翻译
  ])
  
  // 4. 合并应用特定翻译
  const appMessages = (await import(`../messages/${locale}.json`)).default
  
  return {
    messages: {
      ...messages,
      ...appMessages
    }
  }
})
```

#### 使用示例

```typescript
// apps/bridge/app/[locale]/bridge/page.tsx
import { useTranslations } from 'next-intl'

export default function BridgePage() {
  const t = useTranslations()
  
  return (
    <div>
      {/* 通用翻译 */}
      <button>{t('common.button.submit')}</button>
      
      {/* 钱包领域翻译 */}
      <button>{t('wallet.connect')}</button>
      
      {/* 桥接领域翻译 */}
      <h1>{t('bridge.transfer')}</h1>
      
      {/* 应用特定翻译 */}
      <h2>{t('bridge.page_title')}</h2>
    </div>
  )
}
```

---

### package.json 配置示例

#### 根 package.json

```json
{
  "name": "biya-coin",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:helix": "npm run dev --workspace=apps/helix",
    "dev:bridge": "npm run dev --workspace=apps/bridge",
    "build": "npm run build --workspaces --if-present",
    "build:helix": "npm run build --workspace=apps/helix",
    "build:bridge": "npm run build --workspace=apps/bridge"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "typescript": "^5.0.0"
  }
}
```

#### apps/helix/package.json

```json
{
  "name": "@biya/helix",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "next-intl": "^4.4.0",
    
    "@biya/shared": "workspace:*",
    "@biya/theme": "workspace:*",
    "@biya/i18n": "workspace:*",
    "@biya/wallet": "workspace:*",
    "@biya/auth": "workspace:*"
  }
}
```

#### apps/bridge/package.json

```json
{
  "name": "@biya/bridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "next-intl": "^4.4.0",
    
    "@biya/shared": "workspace:*",
    "@biya/theme": "workspace:*",
    "@biya/i18n": "workspace:*",
    "@biya/wallet": "workspace:*",
    
    "@injectivelabs/sdk-ts": "1.16.7",
    "@0xsquid/sdk": "^2.10.2",
    "wagmi": "^2.19.1",
    "viem": "^2.38.5",
    "ethers": "^6.15.0"
  }
}
```

#### packages/shared/package.json

```json
{
  "name": "@biya/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./components": "./components/index.ts",
    "./hooks": "./hooks/index.ts",
    "./utils": "./utils/index.ts"
  },
  "dependencies": {
    "react": "^19.1.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.3.1",
    "lucide-react": "^0.548.0"
  }
}
```

---

### 迁移步骤概览

```
第 1 步: 创建 Monorepo 基础结构
  ├── 创建 apps/ 和 packages/
  ├── 配置 workspaces
  └── 移动 biya-helix-app 到 apps/helix

第 2 步: 提取共享代码到 packages/shared
  ├── 移动 Button.tsx、Card.tsx 等
  ├── 移动 Header.tsx、Footer.tsx 等
  └── 移动通用 Hooks 和工具函数

第 3 步: 创建功能包
  ├── packages/theme（从 ThemeContext 重构）
  ├── packages/i18n（分层翻译架构）
  └── packages/wallet（钱包相关）

第 4 步: 独立 Bridge 应用
  ├── 创建 apps/bridge
  ├── 移动 app/bridge/ 相关代码
  ├── 移动 components/bridge/
  ├── 移动 context/bridge/
  └── 移动 lib/bridge/

第 5 步: 调整依赖和引用
  ├── 更新 import 路径
  ├── 配置 @biya/* 引用
  └── 测试编译

第 6 步: 配置独立部署
  ├── 配置 Vercel 项目
  └── 配置 CI/CD
```

---

## 🚀 下一步

架构设计已明确，继续阅读：

**→ [03-目录结构.md](./03-目录结构.md)**

查看更详细的配置文件和实施步骤。

---

## 📚 本章小结

### 核心概念

1. **Monorepo**: 单一仓库，多个项目
2. **Workspace**: 包含多个 package 的集合
3. **Package**: 独立的项目单元
4. **本地引用**: 使用 `workspace:*` 引用

### 架构原理

1. **依赖提升**: 公共依赖提升到根目录
2. **符号链接**: 本地包使用软链接
3. **包解析**: 优先本地包，再查找 node_modules

### 设计原则

1. **共享 vs 独立**: 根据使用场景决定
2. **分层策略**: 应用层 → 领域层 → 基础层
3. **模块粒度**: 粗粒度优于细粒度
4. **版本管理**: 统一版本优于独立版本

---

*最后更新: 2025-10-31*

